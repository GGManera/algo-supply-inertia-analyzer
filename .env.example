# Algorand Indexer Base URL
INDEXER_BASE=https://mainnet-idx.algonode.cloud

# Minimum balance to track (in ALGO)
THRESHOLD_ALGO=5000

# Dashboard Port
PORT=3000

# Database Path
DB_PATH=./data/supply.db

# Parallel requests to indexer (adjust based on rate limits)
CONCURRENCY=30
